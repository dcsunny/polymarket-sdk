// auth_constants.go 模块
package polymarket

const (
	// ClobAuthDomainName CLOB L1 认证签名的 domain name（与 Node SDK 对齐）。
	ClobAuthDomainName = "ClobAuthDomain"
	// ClobAuthVersion CLOB L1 认证签名的版本号（与 Node SDK 对齐）。
	ClobAuthVersion = "1"
	// ClobAuthMessage CLOB L1 认证签名的提示文案（与 Node SDK 对齐）。
	ClobAuthMessage = "This message attests that I control the given wallet"
)
